export const metadata = {
  title: "Contact",
  description:
    "Get in touch with Erol Cetinok — email or use the contact form.",
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
